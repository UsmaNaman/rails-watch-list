class MoviesController < ApplicationController

  # def search
  #   # Use the omdbapi gem to search for movies matching the specified query
  #   query = params[:query]
  #   service = OmdbapiService.new(api_key: ENV["OMDB_API"])
  #   movies = service.call(query)
  #   # movies = Omdbapi.search(query)

  #   # Render the search results as a JSON response
  #   render json: movies
  # end


end
